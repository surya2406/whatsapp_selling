# Agent orchestration
google-adk>=0.1.0

# LLM — LiteLLM is bundled with google-adk and handles Ollama via ollama_chat/ prefix
litellm>=1.65.5
openai>=1.40.0  # needed by litellm for the openai provider fallback

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Task queue
celery>=5.4.0
redis==4.6.0

# Database - local SQLite agent DB
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0

# Meta Engine DB - MySQL/MariaDB
pymysql>=1.1.0
aiomysql>=0.2.0
cryptography>=42.0.0  # required by pymysql for auth plugins

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# HTTP
httpx>=0.27.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.3.0  # retry logic for LLM calls
